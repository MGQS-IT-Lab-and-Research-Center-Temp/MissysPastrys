@model MissysPastrys.Models.Pastry.PastryViewModel

    @*<div class="card mb-4 shadow-sm">
        <img src="@(string.IsNullOrEmpty(Model.ImageUrl) ? "/Images/noimage.png" : Model.ImageUrl) class="img-thumbnail" />
        <div class="card-body">
            <h3 class="card-title">@(string.IsNullOrEmpty(Model.Name) ? "Name is not available" : Model.Name)</h3>
            <p class="card-text">@(string.IsNullOrEmpty(Model.ShortDescription) ? "Short description is not available" : Model.ShortDescription)</p>
            <div class="d-flex justify-content-between align-items-center">
                <div class="btn-group">
                    <a asp-route="pastryDetailsRoute" asp-route-id="@Model.Id" class="btn btn-sm btn-outline-secondary">View details</a>

                    <a asp-controller="Review" asp-action="GetReviewDetail" asp-route-id="@Model.Id" class="btn btn-sm btn-outline-secondary">View Reviews<span class="badge bg-dark">@Model.Reviews.Count</span></a>
                </div>
            </div>
        </div>
    </div>*@

    <div class="col-sm-4 col-lg-4 col-md-4">
        <div class="img-thumbnail">
            <img src="@Model.ImageUrl" alt="" />
            <div class="caption">
                <h3 class="pull-right">@Model.SellingPrice.ToString("c")</h3>
                <h3>
                    <a asp-controller="Pastry" asp-action="GetPastry" asp-route-id="@Model.Id">@Model.Name</a>
                </h3>
                <p>@Model.ShortDescription</p>
            </div>
            <div class="addToCart text-right">
                <p class="button">
                    <a class="btn btn-success" id="cartButton" asp-controller="ShoppingCart" asp-action="AddToShoppingCart" asp-route-id="@Model.Id">Add to cart</a>
                </p>
            </div>
        </div>
    </div>
